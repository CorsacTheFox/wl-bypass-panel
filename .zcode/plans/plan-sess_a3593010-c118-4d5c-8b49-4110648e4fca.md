## Overview

Add **Telegram-based authorization for an Android app** and a new **session-scoped "connect/disconnect" link model** that distinguishes authorized vs unauthorized clients. Deliverable = updated spec doc **+** backend code.

The Telegram auth infra already exists (`POST /api/auth/telegram`, HMAC initData verifier, `users.telegram_id`, `get_or_create_for_telegram`). This work adds the missing pieces the new behavior requires.

---

## Part A — Backend code changes

### A1. New config keys (`config.py`)
- `APP_DEFAULT_TIMEOUT_SECONDS` (env `WB_APP_DEFAULT_TIMEOUT_SECONDS`, default `86400` = 24h) — the long-lived timeout used by the app's **authorized** connect flow (vs. the existing 1h `DEFAULT_TIMEOUT_SECONDS` and 15-min quick).
- `TELEGRAM_AUTO_CAN_CREATE` (env `WB_TELEGRAM_AUTO_CAN_CREATE`, default `"0"`) → parsed bool. When on, Telegram-auto-created users get `can_create_instances=True` so the app works out-of-the-box (fixes existing gap).

### A2. DB migration (`db.py`)
Additive `ALTER TABLE` guarded by `PRAGMA table_info`, matching the existing migration style:
- `instances.app_session` `INTEGER NOT NULL DEFAULT 0` — flags rows created via the app connect flow (mirrors the existing `is_quick` column pattern). Lets the app flow be tracked/attributed separately.

No table rebuild needed; existing columns already cover everything else.

### A3. Service layer (`services.py`)
1. **`get_or_create_for_telegram`**: honor `TELEGRAM_AUTO_CAN_CREATE` when auto-creating (step 3) — pass `can_create_instances=TELEGRAM_AUTO_CAN_CREATE` instead of hardcoded `False`.
2. **`InstanceService.start`**: accept `app_session: bool = False` and persist it into the row (alongside `is_quick`). No change to quota/privilege logic — authorized app users are normal users subject to `max_concurrent` + `can_create_instances`.
3. **New method `InstanceService.start_or_resume_active`**: the authorized "connect" core.
   - If the user already has an **active** (`pending`/`running`) `app_session` instance **with an `output_link`** → return it (same link, idempotent reconnect; e.g. app re-launched while connected).
   - Else start a new instance with `app_session=True` and the app default timeout.
   - Raises `ForbiddenError`/`ConcurrencyLimitError` like `start`.
4. **New method `InstanceService.stop_app_session`**: stops the user's active `app_session` instance(s) — the "disconnect" core. (Thin filter over the existing `stop`/`stop_all_for_user` logic.)

### A4. New router: `routers/app.py` (prefix `/api/app`, requires any authenticated user)
The two separate GET requests the spec calls for, plus auth wiring:

- **`GET /api/app/connect`** → authorized "get me my link for as long as I'm connected".
  - Calls `start_or_resume_active`; on success waits (polls DB ≤ ~30s, reusing the existing `_wait_for_output_link`-style loop) for `output_link` to appear, then returns:
    `200 { "instance_id": 42, "output_link": "wbstream://…", "status": "running", "expires_at": "<timeout_at>" }`.
  - Error mapping: 403 (privilege), 409 (quota full), 504 (link timeout), 401 (re-auth), 404 (no enabled service).
- **`GET /api/app/disconnect`** → authorized "I'm disconnecting".
  - Calls `stop_app_session`; returns `200 { "stopped": [..ids..], "instance_id": <last> }` (or `{"stopped": []}` if nothing active). Idempotent.

GET (not POST) is used per the user's explicit requirement ("separate Get request ... on connecting and disconnecting"). Both are idempotent so reconnect/disconnect duplicates are safe.

### A5. Discovery + quick-guard
- **`GET /api/config`** (public, in `main.py` or a tiny `routers/public.py`) → `{ "telegram_login_enabled": <bool>, "telegram_bot_username": <from a new env `WB_TELEGRAM_BOT_USERNAME`> }`. Never returns the token. (Fills the existing spec gap §10.2.)
- **`QUICK_TOKEN` enforcement on `/api/quick/*`**: the existing `WB_QUICK_TOKEN` exists but `quick.py` ignores it. Add a dependency that, **when `QUICK_TOKEN` is set**, requires `X-Api-Key` header match (or `?token=`) — turns the unauthorized client's "regular temporary link" from fully-public into guarded, matching the spec §10.3 recommendation. When unset, behavior is unchanged (open) for dev.

### A6. Settings + admin wiring
- Add `telegram_auto_can_create` and `app_default_timeout_seconds` to `SettingsStore._DEFAULTS` so they're runtime-tunable from `GET/PUT /api/admin/settings` (and env fallback), consistent with `quick_*`. Extend the admin `SettingsUpdate` pydantic model with these two fields.
- Mount the new router in `main.py` alongside the others.

### A7. `.env.example` / `env.example`
Document the new env vars (`WB_TELEGRAM_BOT_USERNAME`, `WB_TELEGRAM_AUTO_CAN_CREATE`, `WB_APP_DEFAULT_TIMEOUT_SECONDS`).

**No changes** to `process_manager.py` — link extraction, timeouts, and reaping already work for any instance; the app-session instances are normal instances.

---

## Part B — Spec doc (`docs/ANDROID_APP_SPEC.md`)

Extend the existing draft (v1.0 → v1.1) to fully describe the new behavior. Add:

1. **New §X "Authorization model (Telegram)"** — concise restatement of `POST /api/auth/telegram` (Mini App initData flow), `GET /api/config` discovery, token storage, re-auth on 401, account-linking semantics, and the auto-grant setting. (Consolidates/describes the auth methods explicitly, as requested.)
2. **New §Y "Connection lifecycle & link model"** — the core of the new requirement:
   - **Authorized client**: `GET /api/app/connect` on connect → long-lived instance + same `output_link` for the whole connection; `GET /api/app/disconnect` on disconnect → stops it. Exact request/response JSON, status codes, idempotency (reconnect returns the same link), and how "same link" is guaranteed (resume-active branch).
   - **Unauthorized client**: `GET /api/quick/link` (15-min, global cap, optional `X-Api-Key`). A regular temporary link, identical to any other anonymous caller.
   - Comparison table (authorized vs unauthorized: lifetime, attribution, quota, auth required).
   - End-to-end ASCII sequences for both.
   - Note that authorized users are still subject to `max_concurrent` / `can_create_instances` and that a brand-new TG user may hit 403 until auto-grant is on / admin grants.
3. Update §10 ("Required backend adjustments") to mark the now-implemented items (auto-grant, `/api/config`, quick-guard, connect/disconnect endpoints) as **done** with pointers to the new code, and bump the version header.

I'll match the existing doc style (English, markdown, tables, fenced HTTP examples, ASCII sequences, `[BACKEND CHANGE]` markers).

---

## Why this design
- **Authorized link = a normal long-lived instance, controlled by connect/disconnect GETs.** Reuses `start`/`stop`/`schedule_timeout`/`_wait_for_output_link` entirely — no new process-management code, no new transport concept. The `app_session` flag just attributes/tracks it.
- **"Same link while connected"** = the resume-active branch in `start_or_resume_active`: if a live app-session instance with a link already exists, return it instead of starting a new one. This is what makes reconnect idempotent and guarantees one link per connection.
- **Unauthorized = unchanged quick flow** (+ optional key) — exactly "a regular temporary link like anyone else."
- All new endpoints follow existing patterns (FastAPI dependency auth, `SettingsStore` for tunables, additive DB migration, thin router over service layer).

## Out of scope
- The Android APK itself (no Android code in this repo; spec defines the contract).
- Re-implementing the WebRTC transport (consumed via `output_link` handoff).
- Tests (repo has none today; I won't introduce a test framework unless asked — but I can add a small FastAPI `TestClient` smoke test if you want).

## Files touched
- `config.py`, `db.py`, `services.py`, `main.py`, `routers/app.py` (new), `routers/quick.py` (token guard), `routers/admin.py` (settings fields), `.env.example` / `env.example`, `docs/ANDROID_APP_SPEC.md`.